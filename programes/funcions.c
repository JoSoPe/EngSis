void setup();//configurations 
lect sensordata(lect a);//Read sensor and stores values into a.
void toprint(lect a);//Print the values of a.x,a.y and a.z
bool dart(lect a,char eix); //check for a dart in the specified eix
